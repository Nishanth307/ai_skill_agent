
# requirements.txt

# --- LangChain Core ---
langchain>=0.3.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# --- LangGraph ---
langgraph>=0.2.0
langgraph-checkpoint-redis>=0.1.0  # Redis checkpointer for LangGraph

# --- LLM Backends ---
langchain-google-genai>=2.0.0      # Gemini
langchain-ollama>=0.2.0            # Ollama (local)

# --- Vector Store & Embeddings ---
chromadb>=0.5.0                    # Local vector DB

# --- Structured Output & Validation ---
pydantic>=2.0.0
pydantic-settings>=2.0.0           # Settings management via .env


# --- Relational DB (REMOVED) ---
# Using Redis for transient session storage

# --- Cache & Session State ---
redis>=5.0.0                       # Redis client
hiredis>=3.0.0                     # Faster Redis parsing

# --- API Layer ---
fastapi>=0.115.0
uvicorn>=0.32.0
python-multipart>=0.0.12           # For file uploads (resume PDF)
httpx>=0.28.0                      # Async HTTP client

# --- UI ---
streamlit>=1.40.0

# --- Document Parsing ---
pypdf>=5.0.0                       # PDF resume parsing
python-docx>=1.1.0                 # Word doc resume parsing

# --- Observability ---
langsmith>=0.2.0                   # LangGraph tracing & debugging

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.24.0

# --- Utilities ---
python-dotenv>=1.0.0
email-validator>=2.0.0
sentence-transformers>=3.0.0
